{% for category in categories %}
  <a class="button ll-category--{{ category.slug }}" href="#{{ category.slug }}" data-ll-category="{{ category.slug }}">
    {{ category.name }}
    <small>{{ category.total }}
      calls</small>
  </a>
{% endfor %}