{% extends "card.njk" %}

{% block content %}
  <div class="ll-card--{{ item.type }}__content">
    <h2 class="visually-hidden sr-only">{{ category.name }}</h2>
    <p>
      {{ item.text }}
    </p>
  </div>

{% endblock %}